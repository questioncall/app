diff --git a/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LiveKitReactNative.kt b/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LiveKitReactNative.kt
index 01b6e76..a1915a8 100644
--- a/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LiveKitReactNative.kt
+++ b/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LiveKitReactNative.kt
@@ -98,6 +98,11 @@ object LiveKitReactNative {
 
         _audioProcessingController = null
 
+        // Guard: if setup() was never called (e.g. during Metro hot-reload the singleton
+        // state may be reset before setup() re-runs), skip re-initialization to avoid
+        // IllegalStateException from audioRecordSamplesDispatcher being null.
+        if (_audioRecordSamplesDispatcher == null) return
+
         // adm needs to be re-setup with a new one each time.
         setupAdm(context)
         options.audioDeviceModule = adm
diff --git a/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LivekitReactNativeModule.kt b/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LivekitReactNativeModule.kt
index f78e728..3cf5d54 100644
--- a/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LivekitReactNativeModule.kt
+++ b/node_modules/@livekit/react-native/android/src/main/java/com/livekit/reactnative/LivekitReactNativeModule.kt
@@ -230,6 +230,10 @@ class LivekitReactNativeModule(reactContext: ReactApplicationContext) : ReactCon
     }
 
     override fun invalidate() {
-        LiveKitReactNative.invalidate(reactApplicationContext)
+        try {
+            LiveKitReactNative.invalidate(reactApplicationContext)
+        } catch (e: IllegalStateException) {
+            Log.w(name, "Ignoring IllegalStateException during module invalidation (hot-reload): ${e.message}")
+        }
     }
 }
