# Step 1 - Install dev client

npx expo install expo-dev-client

# Step 2 - Run on Android

npx expo run:android

# Step 3 (the one you're asking about) - Start with dev client

npx expo start --dev-client
